数据操作---------------------------------------------------------------------------------------------

new_array = array.astype(np.float)

取索引
1.  切片索引
2.  布尔掩码(mask)索引 利用 ~ | &
    data[data < 0] = 0
3.  传入列表取多行/列，或作为点坐标